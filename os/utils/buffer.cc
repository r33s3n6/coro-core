#include "buffer.h"

#include <utils/log.h>
#include <utils/assert.h>

/*
template <typename buffer_t, int32 min_buffer_count, int32 max_buffer_count>
template <typename ...Args>
task<typename buffer_manager<buffer_t,min_buffer_count,max_buffer_count>::buffer_ptr_t> 
buffer_manager<buffer_t,min_buffer_count,max_buffer_count>::get_node(Args&&... args) {
*/
   